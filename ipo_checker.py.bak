#! /usr/bin/python

import argparse, requests

def run_ipo_checker(args):


if __name__ == '__main__' :
    parser = argparse.ArgumentParser(description = 'IPO Checker for a list.')
    parser.add_argument('--company', type = str, required = True, help = 'Comany ID')
    parser.add_argument('--file', type = str, required = True, help = 'Name of the pattern file.')
    args = parser.parse_args()

    run_ipo_checker(args);